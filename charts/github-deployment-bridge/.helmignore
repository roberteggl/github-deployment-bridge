# SPDX-FileCopyrightText: 2026 Robert Eggl <robert@eggl.dev>
#
# SPDX-License-Identifier: Apache-2.0

.DS_Store
.git
# Do not ignore README.md — Artifact Hub and `helm show readme` need it.
# Avoid `*.md` + `!README.md`: Helm's ignore matcher treats that as excluding
# Chart.yaml (chart load fails with "Chart.yaml file is missing").
