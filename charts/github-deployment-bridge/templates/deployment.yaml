# SPDX-FileCopyrightText: 2026 Robert Eggl <robert@eggl.dev>
#
# SPDX-License-Identifier: Apache-2.0

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "github-deployment-bridge.fullname" . }}
  labels:
    {{- include "github-deployment-bridge.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  {{- if .Values.persistence.enabled }}
  strategy:
    type: Recreate
  {{- end }}
  selector:
    matchLabels:
      {{- include "github-deployment-bridge.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        checksum/config: {{ include "github-deployment-bridge.configChecksum" . | sha256sum }}
        {{- if not .Values.github.existingSecret }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        {{- end }}
      labels:
        {{- include "github-deployment-bridge.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "github-deployment-bridge.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: bridge
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: metrics
              containerPort: {{ .Values.containerPorts.metrics }}
              protocol: TCP
            - name: probes
              containerPort: {{ .Values.containerPorts.probes }}
              protocol: TCP
          env:
            - name: CLUSTER_NAME
              value: {{ .Values.config.clusterName | quote }}
            - name: ENVIRONMENT
              value: {{ .Values.config.environment | quote }}
            - name: WATCH_NAMESPACE
              value: {{ .Values.config.watchNamespace | quote }}
            - name: DATABASE
              value: /data/cache.db
            - name: ENVIRONMENT_URL
              value: {{ .Values.config.environmentURL | quote }}
            - name: DESCRIPTION
              value: {{ .Values.config.description | quote }}
            - name: LOG_URL_TEMPLATE
              value: {{ .Values.config.logURLTemplate | quote }}
            - name: LOG_URL_TEMPLATE_ESCAPE
              value: {{ .Values.config.logURLTemplateEscape | quote }}
            - name: LOG_LEVEL
              value: {{ .Values.config.logLevel | default "info" | quote }}
            - name: METRICS_ADDR
              value: {{ printf ":%v" .Values.containerPorts.metrics | quote }}
            - name: PROBE_ADDR
              value: {{ printf ":%v" .Values.containerPorts.probes | quote }}
            - name: LEADER_ELECTION
              value: {{ .Values.config.leaderElection | quote }}
            - name: RETRY_MAX_ATTEMPTS
              value: {{ .Values.config.retry.maxAttempts | quote }}
            - name: RETRY_INITIAL_BACKOFF
              value: {{ .Values.config.retry.initialBackoff | quote }}
            - name: RETRY_MAX_BACKOFF
              value: {{ .Values.config.retry.maxBackoff | quote }}
            - name: GITHUB_APP_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "github-deployment-bridge.secretName" . }}
                  key: app-id
            - name: GITHUB_INSTALLATION_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "github-deployment-bridge.secretName" . }}
                  key: installation-id
                  optional: true
            - name: GITHUB_INSTALLATION_CACHE_TTL
              value: {{ .Values.config.githubInstallationCacheTTL | quote }}
            - name: GITHUB_PRIVATE_KEY_PATH
              value: /github/private-key.pem
            {{- if .Values.config.githubBaseURL }}
            - name: GITHUB_BASE_URL
              value: {{ .Values.config.githubBaseURL | quote }}
            {{- end }}
            {{- if .Values.registry.existingDockerConfigSecret }}
            - name: DOCKER_CONFIG
              value: /var/run/secrets/docker
            {{- end }}
          volumeMounts:
            - name: data
              mountPath: /data
            - name: github
              mountPath: /github
              readOnly: true
            {{- if .Values.registry.existingDockerConfigSecret }}
            - name: docker-config
              mountPath: /var/run/secrets/docker
              readOnly: true
            {{- end }}
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.liveness.path }}
              port: {{ .Values.probes.liveness.port }}
            initialDelaySeconds: 5
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readiness.path }}
              port: {{ .Values.probes.readiness.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: data
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "github-deployment-bridge.fullname" . }}-data
          {{- else }}
          emptyDir: {}
          {{- end }}
        - name: github
          secret:
            secretName: {{ include "github-deployment-bridge.secretName" . }}
            items:
              - key: private-key
                path: private-key.pem
        {{- if .Values.registry.existingDockerConfigSecret }}
        - name: docker-config
          secret:
            secretName: {{ .Values.registry.existingDockerConfigSecret | quote }}
            items:
              - key: {{ .Values.registry.dockerConfigKey | quote }}
                path: config.json
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
